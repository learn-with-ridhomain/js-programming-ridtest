/** 
 * Make a function that takes 1 string and 1 number, and return true if the first argument is string and the second argument is number
 * Example: example('blabla', 2) will return true, and example2(2, 'blabla') will return false
 * Input : 1 string, 1 number
 * Output: boolean
 */

const solution = () => {
  // Write the solution here
};

module.exports = {
  solution
};